# customized-shortcode-forms
A customized shortcode that have a validation and full CRUD functionality wit database. Also WordPress classes are implemented and overrided, in addition to customized classes.
